// Foreign library used by the pipeline Worker. It has no externs or Wasm imports.
pkgtype(kind: "foreign_library")

import {
  "moonbitlang/core/math",
}

import {
  "moonbitlang/core/bench",
} for "wbtest"

options(
  link: { "wasm": { "export-memory-name": "memory" } },
)
