name = "sf-atools/dsp"

version = "0.1.0"

preferred_target = "wasm"

description = "Numeric kernel of atools: FFT, STFT codec and phase reconstruction, compiled to WASM for direct browser use"

license = "Apache-2.0"

keywords = [ "wasm", "fft", "stft", "phase-retrieval", "simd" ]
