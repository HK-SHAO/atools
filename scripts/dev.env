# dev 服务器要注入浏览器的常量（`bunfig.toml` 的 `[serve.static] env` 只认进程启动时就存在的
# 环境变量，所以只能由 `bun --env-file` 在启动前送进来 —— 见 package.json 的 `dev`）。
#
# `app/ui/pipeline.ts` 拿它当数值 Worker 的地址，`scripts/serve.ts` 拿它当要供的那条路由。
# 名字必须与 Worker 的入口文件名一致（`app/ui/pipeline.worker.ts` → `pipeline.worker.js`）：
# dev 里这条 URL 是写死的，产物名跟着它走。
PIPELINE_WORKER=/worker/pipeline.worker.js
